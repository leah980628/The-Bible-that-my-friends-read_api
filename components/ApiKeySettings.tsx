import React from 'react';

// 이 파일은 더 이상 사용되지 않지만, 빌드 시스템에서 파일 참조 오류를 방지하기 위해 
// 빈 컴포넌트(Dummy Component)로 유지합니다.
// @google/genai 라이브러리 의존성은 제거되었습니다.

interface ApiKeySettingsProps {
  onClose: () => void;
}

export const ApiKeySettings: React.FC<ApiKeySettingsProps> = ({ onClose }) => {
  return null;
};